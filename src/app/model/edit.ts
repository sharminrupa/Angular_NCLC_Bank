export class AccEdit{
    id: number;
    name: string;
    gender: string;
    mobile: string;
    address: string;
    date: string;
    openingbalance: string;

    constructor(id, name, gender, mobile, address, date, openingbalance){
            this.id = id;
            this.name = name;
            this.gender = gender;
            this.mobile = mobile;
            this.address = address;
            this.date = date;
            this.openingbalance = openingbalance;
        }
    }
